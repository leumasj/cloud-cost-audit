{
  "name": "cloud-cost-audit",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@vercel/speed-insights": "^1.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.2.0",
    "@anthropic-ai/sdk": "^0.39.0",
    "@sendgrid/mail": "^8.1.3",
    "stripe": "^14.21.0"
  },
  "_serverDependencies_note": "The following are used only in /api/ serverless functions and intentionally kept in devDependencies to avoid bundling in frontend: @anthropic-ai/sdk, @sendgrid/mail, stripe"
}